import {defaultProps as iDefaultProps} from '../Input/defaultTheme'

export {getPlaceholderClass, getHoverClass, getFocusClass, colors} from '../Input/defaultTheme'
export const defaultProps = {
  ...iDefaultProps,
  br: 2
}
