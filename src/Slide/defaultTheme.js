import {defaultSpeed, defaultEasing} from '../Fade/defaultTheme'
export {defaultSpeed, defaultEasing} from '../Fade/defaultTheme'
export const defaultDirection = 'fromTop'

export default {defaultDirection, defaultSpeed, defaultEasing}
